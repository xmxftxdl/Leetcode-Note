class Solution {
public:
    void rotate(vector<int>& nums, int k) {
        int temp;
        for(int i=0;i<k;i++){
            temp=nums.back();
            nums.pop_back();
            nums.insert(nums.begin(),temp);
        }
    }
};
